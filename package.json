{
  "name": "better-than-nothing-ci",
  "version": "0.5.0",
  "description": "BetterThanNothing CI: a build system that's better than nothing! Provides a way for you to automate console commands in a folder (on a cron schedule) and view the output in a browser.",
  "main": "server/server.js",
  "scripts": {
    "server-build": "cd server && tsc",
    "server-start": "cd server && tsc && node server.js",
    "client-start": "cd client && ng serve",
    "client-dev": "cd client && ng build",
    "client-prod": "cd client && ng build --prod"
  },
  "keywords": [
    "ci",
    "build system"
  ],
  "author": "Luke Zehrung",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {}
}
