{
  "name": "better-than-nothing-ci",
  "version": "1.0.0",
  "description": "BetterThanNothingCI: a CI server that's better than nothing! Provides a way for you to automate console commands in a folder (on a cron schedule) and view the output in a browser.",
  "main": "server.js",
  "scripts": {
    "start": "tsc && node server.js",
    "build": "cd client && ng build",
    "prod": "cd client && ng build --prod"
  },
  "keywords": [],
  "author": "Luke Zehrung",
  "license": "MIT",
  "dependencies": {
    "@sendgrid/mail": "^6.3.1",
    "cross-spawn": "^6.0.5",
    "express": "^4.16.3",
    "node-schedule": "^1.3.0",
    "pug": "^2.0.3"
  },
  "devDependencies": {
    "@types/express": "^4.16.0",
    "@types/node-schedule": "^1.2.2",
    "@types/cross-spawn": "^6.0.0"
  }
}
