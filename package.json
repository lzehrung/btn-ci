{
  "name": "better-than-nothing-ci",
  "version": "1.0.0",
  "description": "BetterThanNothingCI: a CI server that's better than nothing! This is an ExpressJS app that periodically runs a git pull, ionic build, our unit tests and then alerts of failure.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "cd client && ng build",
    "prod": "cd client && ng build --prod"
  },
  "keywords": [],
  "author": "Luke Zehrung",
  "license": "MIT",
  "dependencies": {
    "cross-spawn": "^6.0.5",
    "express": "^4.16.3",
    "node-schedule": "^1.3.0",
    "nodemailer": "^4.6.7",
    "pug": "^2.0.3"
  },
  "devDependencies": {
    "@types/express": "^4.16.0"
  }
}
